{
  "name": "submisions",
  "version": "1.0.1",
  "description": "A submissions project",
  "main": "index.js",
  "scripts": {
    "install": "cd api && npm install && cd .. && cd web && npm install && cd ..",
    "start": "concurrently --kill-others \"cd api && npm start\" \"cd web && npm start\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^4.1.0",
    "nodemon": "^1.18.11"
  },
  "dependencies": {
    "@okta/okta-auth-js": "^2.5.0",
    "@okta/okta-react": "^1.2.0",
    "react-number-format": "^4.0.8",
    "react-router-dom": "^5.0.0"
  }
}
